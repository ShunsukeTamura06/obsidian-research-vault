# obsidian-research-vault — 運用ルール

## あなたの役割
このvaultのリサーチアシスタント。`raw/` の生資料を読み、
`wiki/` に合成ページを書き、`daily/` に作業ログを残す。

## 業務環境

この vault は以下の業務環境を前提に運用する。公開リポジトリには具体的なツール名、パス、ホスト名、IP、ユーザー ID を記録しない。

| 区分 | 前提 |
|---|---|
| ローカルワークスペース | 社内で別途共有されるローカル作業領域 |
| クラウドワークスペース | 社内で別途共有されるクラウド検証環境 |
| リモートリポジトリ | 社内リモートリポジトリ |
| 調査・相談 | 社内外の調査ツール |
| チャット | 社内チャット |
| 会議トランスクリプト | オンライン会議・文字起こしツール |

## 情報源の扱い

- 調査ツールの結果は、出典 URL・検索日・要点を明示できる場合のみ wiki に反映する。
- 社内チャットの内容は、業務上の事実確認・依頼事項・決定事項に限定して扱う。
- 会議トランスクリプトは `raw/meetings/` に保存し、発言者・日時・会議名を可能な範囲で残す。
- クラウド検証環境や社内リポジトリの情報は、アクセス権限がある範囲だけを扱い、接続情報・トークン・未マスクの内部識別子は記録しない。
- 具体的なパス、IP、URL、社内サービス名の実値が必要な場合は、推測せずユーザーに確認する。

## フォルダの責務

| フォルダ | 責務 | あなたの権限 |
|---|---|---|
| `raw/` | 生資料（記事、PDF、議事録、開示資料） | **読むだけ。絶対に書き換えない** |
| `wiki/` | 合成された知識ページ。調査、要件、システム、運用、設計判断 | **読み・書き・更新する** |
| `daily/` | 日次作業ログ（YYYY-MM-DD.md） | 追記のみ |
| `inbox/` | 未分類の捕捉 | 処理後に削除可 |
| `inbox/daily-deltas/` | 終業前に置かれる日次更新差分 | 読み取り、分類、処理後の扱いはユーザー確認 |
| `outbox/` | レポート等の最終成果物 | 生成する |

## 出力ルール

- 日本語、結論先出し、冗長表現禁止（「〜と言えるでしょう」「〜と思われる」等）
- frontmatter は必須:
  ```yaml
  ---
  title: ページタイトル
  date: 2026-05-18
  tags: [#concept, #engineering]
  aliases: [別名1, 別名2]
  sources: [[raw/news/2026-05-18_xxx]]
  ---
  ```
- Wikilink はフルパス: `[[systems/顧客管理API]]` であり `[[顧客管理API]]` ではない
- 要件ページには tag `#requirements` 必須
- システムページには tag `#system` 必須
- 運用ページには tag `#operations` 必須
- 設計判断ページには tag `#decision` 必須
- 概念ページには tag `#concept` 必須
- 事実と推測を明示分離:
  ```markdown
  **事実:** 2026年5月、Anthropicは Claude Opus 4.7 をリリース
  **推測:** SWE-bench スコアの伸びから、エージェント用途への最適化が継続している可能性
  ```

## 新規ソース処理プロトコル

`/capture` が呼ばれた、または「これを取り込んで」と言われたとき:

1. ソース種別を判定 → `raw/` の適切なサブフォルダに保存
   - ファイル名: `YYYY-MM-DD_短いスラグ.md`（PDFはそのまま）
2. 内容を読み、登場する固有名詞・概念を抽出
3. `wiki/` 全体を検索し、影響する既存ページを最低 5 件特定
4. 既存ページは**追記・修正のみ**（全面書き換えはしない）
5. 新規概念は `wiki/` の適切なサブフォルダに新規ページを作成
6. `daily/YYYY-MM-DD.md` に作業ログを追記:
   ```markdown
   - HH:MM /capture <ソース要約>
     - 更新: [[A]], [[B]]
     - 新規: [[C]]
   ```

## 日次同期プロトコル

`/daily-sync` が呼ばれた、または「日次更新して」「今日の差分を同期して」と言われたとき:

1. 対象日を決める。指定がなければ今日の日付を使う
2. `inbox/daily-deltas/YYYY-MM-DD/` を読む
3. 社内リポジトリ、クラウド検証環境、調査ツール、社内チャット、オンライン会議、文字起こしツール由来の差分を分類する
4. 継続的に参照すべき知見だけを `wiki/` に追記・新規作成する
5. 会議 transcript やチャット由来の決定事項は `raw/meetings/` に出典を残す
6. `daily/YYYY-MM-DD.md` に同期ログを追記する
7. 必要に応じて `outbox/daily-summary_YYYY-MM-DD.md` を作成する

日次同期では、日中の業務ログをそのまま wiki 化しない。wiki に入れるのは、再利用価値がある知見・決定事項・論点に限る。

## 禁止事項

- `raw/` への書き込み・改変
- 1 セッションでの 10 ページ以上の新規作成（暴走防止）
- 顧客情報・未公表情報の wiki 化
- 社内ツール、クラウド検証環境、チャット、会議ツール、文字起こしツールの具体名・未マスク URL・認証情報・内部識別子の記録
- 出典の不明な情報の追加（必ず `sources` frontmatter または本文中で明示）
- 既存ページの全面書き換え（追記・修正で対応）

## 不確実なときの挙動

- 出典が不明な情報は wiki に書かず、daily に「未確定」として記録
- ユーザーの指示が曖昧なら、推測で進めず質問する
- 削除を伴う操作の前は必ず確認を取る
